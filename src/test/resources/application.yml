spring:
  application: blog-luisjrz96
  config:
    activate:
      on-profile: test
  main:
    banner-mode: off

  datasource:
    url: jdbc:h2:mem:testing_db
    driverClassName: org.h2.Driver
    username: sa
    password: password
    ddl-auto: create
  h2:
    console:
      enabled: true
      path: /h2-console